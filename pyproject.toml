[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cli-task-tracker"
version = "0.1.0"
requires-python = ">=3.13"

[project.scripts]
task-tracker = "cli_task_tracker.core:main"

[tool.setuptools]
packages = ["cli_task_tracker"]